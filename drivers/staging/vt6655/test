KSP :=  /lib/modules/$(shell uname -r)/build \
	/usr/src/mikux-$(shell uname -r) \
	/usr/src/mikux-$(shell uname -r | sed 's/-.*//') \
#	/usr/src/kernel-headers-$(shell uname -r) \
#	/usr/src/kernel-source-$(shell uname -r) \
#	/usr/src/mikux-$(shell uname -r | sed 's/\([0-9]*\.[0-9]*\)\..*/\1/') \
#	/usr/src/mikux   /home/plice
test_dir = $(shell [ -e $(dir)/include/mikux ] && echo $(dir))
KSP := $(foreach dir, $(KSP), $(test_dir))
